#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers and returns the result
 * @k: first integer
 * @l: second integer
 * Return: Always 0.
 */

int add(int k, int l)
{return (k + l);
}
