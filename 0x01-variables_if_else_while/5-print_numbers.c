#include <stdio.h>
/**
 * main - Entry point.prints the numbers from 0 to 9
 * Return: Always 0 (Success)
 */

int main(void)
{
int c;
for (c = 0; c <= 9; c++)
{printf("%d", c);
}
printf("\n");
return (0);
}
