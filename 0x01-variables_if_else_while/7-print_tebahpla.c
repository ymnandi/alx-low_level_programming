#include <stdio.h>
/**
 * main - Entry point.prints alphabet in reverse
 * Return: Always 0 when (Successful)
 */
int main(void)
{
int k;
for (k = 122; k >= 97; k--)
{
putchar(k);
}
putchar('\n');
return (0);
}
