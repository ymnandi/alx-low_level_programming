#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point.prints number from 0 to 9
 * Return: Always 0 (Success)
 */
int main(void)
{
int v;
for (v = 0; v <= 10; v++)
putchar(v);
putchar('\n');
return (0);
}
